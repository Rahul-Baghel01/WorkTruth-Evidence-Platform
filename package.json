{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "dev": "pnpm --parallel --filter @workspace/worktruth --filter @workspace/api-server run dev",
    "dev:frontend": "pnpm --filter @workspace/worktruth run dev",
    "dev:api": "pnpm --filter @workspace/api-server run dev",
    "db:push": "pnpm --filter @workspace/db run push",
    "build:api": "pnpm --filter @workspace/api-server run build",
    "build:frontend": "pnpm --filter @workspace/worktruth run build",
    "start:api": "pnpm --filter @workspace/api-server run start",
    "db:up": "docker compose up -d --wait",
    "db:down": "docker compose down",
    "db:seed": "pnpm --filter @workspace/api-server run seed",
    "db:reanalyze": "pnpm --filter @workspace/api-server run reanalyze",
    "db:setup": "pnpm run db:push && pnpm run db:seed",
    "db:reset": "docker compose down -v && pnpm run db:up && pnpm run db:setup",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "test": "pnpm -r --if-present run test"
  },
  "private": true,
  "packageManager": "pnpm@10.4.1",
  "engines": {
    "node": ">=22.9.0"
  },
  "devDependencies": {
    "prettier": "^3.9.6",
    "typescript": "~5.9.3"
  }
}
